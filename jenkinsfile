pipeline {
    agent any

    stages {
        stage('Hello') {
            steps {
                sshGet remote: remote, from: 'abc.sh', into: 'abc_get.sh', override: true
            }
        }
    }
}