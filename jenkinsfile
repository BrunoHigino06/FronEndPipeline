pipeline {
  agent any

  stages {
    stage('Hello') {
     steps { sshagent (credentials: ['SSHAcess']) { 
      sh "scp -o UserKnownHostsFile=/dev/null -o StrictHostKeyChecking=no -r build/libs/ROOT.war ec2-user@ec2-server-domain://tmp"
    
        }
      }
    }
  } 

} 