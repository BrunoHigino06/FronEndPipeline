pipeline {
  agent {
      docker { image 'ubuntu:latest' }
  }
  stages {
      stage('Copy') {
          steps {
              sh 'echo "Copy new deploy to directory"'
              sh 'scp ./index.nginx-debian.html root@172.17.0.4:/var/www/html/'
      }
    }
  }
}